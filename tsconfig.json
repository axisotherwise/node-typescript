{
  "compilerOptions": {
    "strict": true, // only
    "esModuleInterop": true, // choice true ㅡ> import * as X 
    "lib": ["ES2015", "ES2020"], // only
    "moduleResolution": "node", // only
    "typeRoots": ["./server/src/type"],
    "outDir": "./server/src/dist"
  },
  "include": ["./server/src/**/*.ts"],
  
}